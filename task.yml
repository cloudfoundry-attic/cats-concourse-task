---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: relintdockerhubpushbot/cat-concourse-task

inputs:
- name: runtime-ci
- name: cf-acceptance-tests
- name: integration-config
run:
  path: cat-concourse-task/task

params:
  NODES: 12
  CONFIG_FILE_PATH: integration_config.json
